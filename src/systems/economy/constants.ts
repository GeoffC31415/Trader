// Re-export economy_constants for backward compatibility
// New code should use gameConfig from '../../config/game_config'
export { economy_constants } from '../economy_constants';

// Re-export config accessors for convenience
export { getEconomyConfig } from '../../config/game_config';


